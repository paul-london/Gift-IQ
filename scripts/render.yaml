services:
  - type: web
    name: smart-gift-planner
    env: python
    region: virginia
    buildCommand: "python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    plan: free
    autoDeploy: true